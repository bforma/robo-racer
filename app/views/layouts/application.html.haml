!!!
%html
  %head
    %title Robo Racer

    = stylesheet_link_tag "application", media: "all"

    = javascript_include_tag "es5-shim" if Rails.env.test?
    = javascript_include_tag "application"

    = csrf_meta_tags

  %body
    %header.mod-header
      .layout-wrap
        %div
          = link_to "Robo Racer", root_path, class: "logo"

        %nav
          %a Play now!

          - if current_player
            %span.player-name= current_player.name
            = link_to "Log out", destroy_player_session_path, method: :delete

          - else
            = link_to "Log in", new_player_session_path
            = link_to "Sign up", new_player_registration_path

    .mod-body
      .layout-wrap
        .content
          = flash[:notice] if flash[:notice]
          = yield

    %footer.mod-footer
      .layout-wrap
        .content
          :markdown
            Robo Racer © 2015.
            Built with love by [Zilverline](https://www.zilverline.com).
